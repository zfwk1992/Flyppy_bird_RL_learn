# LinkedIn Demo 计划

> 目标：做一个不用安装、点链接就能玩的网页，让人和训练好的 AI 同台竞技，
> 顺带展示这个项目背后的 RL 工作。受众是 LinkedIn 上的招聘方 / 同行。

---

## 一、必须先修正的一个前提

**LinkedIn 的帖子里不能嵌入可交互内容。** 帖子只能渲染链接预览卡片
（一张图 / 一段视频 + 标题 + 描述），不跑 iframe、不跑 JS。
所以「用户直接在 post 上玩」做不到。

现实的路径是：

```
帖子里放一段 30 秒视频（原生视频，信息流里自动播放）
   ↓  正文写钩子 + 结论数字
   ↓  链接放在第一条评论（LinkedIn 算法会压低带外链的帖子曝光）
用户点进去 → 静态网页 → 在那里同台竞技
```

好消息：这段视频**已经有了** —— `demo_wechat_30s.mp4`（1.2 MB，H.264，
576×1024 竖屏），就是那局 100 根管道的结尾。

---

## 二、要不要服务器：不要

**整个 demo 可以是纯静态的，零后端、零运行成本。**

理由是这个模型小到可以塞进浏览器：

| 项 | 数值 |
|---|---|
| 参数量 | 1,258,659 |
| fp32 | 5.03 MB |
| **fp16** | **2.52 MB** |
| int8 量化 | 约 1.26 MB |
| 单次前向计算量 | 约 1230 万次乘加 |
| 需要的推理频率 | 7.5 次/秒（30 FPS ÷ frame_skip 4）|

也就是每秒约 9200 万次乘加 —— 浏览器用 WebGL 或 WASM 轻松够，
甚至纯 JS 都跑得动。**没有任何需要服务器算的东西。**

托管选项（都免费）：

| 方案 | 成本 | 说明 |
|---|---|---|
| **Cloudflare Pages** | $0 | 推荐。全球 CDN，自动 HTTPS，直接连 GitHub 仓库 |
| GitHub Pages | $0 | 同样够用，国内访问略慢 |
| 自定义域名 | 约 $10/年 | 可选。`yourname.dev` 之类，简历上更好看 |

**唯一可能花钱的只有域名。** 不需要任何云主机、容器、数据库。

---

## 三、真正的技术难点（原计划没提到）

难的不是跑模型，是**在浏览器里重建模型认识的那个世界**。

模型吃的是 4 帧 80×128 的二值图，由 pygame 渲染 288×512 画面后降采样得到。
浏览器要复刻的是这条链路：

```
Canvas 288×512  →  取单通道  →  降采样到 80×128  →  二值化  →  叠 4 帧
```

任何一步对不上，模型看到的就是训练时没见过的分布，表现会崩。

### 已经验证的两件事

**① 背景是纯黑的**（`assets/sprites/background-black.png`），
所以二值化后只有管道、小鸟、地面是白的。Canvas 端只要画这三样。

**② 降采样不必复刻 cv2 的 INTER_AREA。**

原以为要精确匹配 OpenCV 的重采样算法。实测发现，因为二值化阈值是 1，
效果等价于「这个格子里有没有非黑像素」—— 一个覆盖判定。
两种方式的像素差异只有 0.18%，而且模型完全不受影响：

| 观测来源 | 平均过管道 | 中位数 | 最高 |
|---|---|---|---|
| cv2 INTER_AREA（真实管线） | 81.25 | 55.0 | 304 |
| 覆盖判定（浏览器式） | 75.83 | 60.0 | 384 |

40 局样本、方差本来就大，两者统计上无法区分。
**这把移植里最大的未知数消掉了。**

### 剩下的风险

物理必须逐位一致：重力 `+0.5/帧`、扇翅 `velY = -5`、终端速度 `5`、
管道 `-5/帧`、`frame_skip=4`。这些都简单，但**必须做一致性验证** ——
用同一个随机种子在 Python 和 JS 各跑一局，逐决策比对动作序列。
这个项目的单测 8 就是干这事的（比对新旧观测管线），照搬那个思路。

---

## 四、界面：左右分屏

**玩家一屏、AI 一屏，左右并排。** 各自独立的游戏实例、独立的画布、独立的计分，
共用同一个种子所以管道序列完全相同 —— 同一张地图上直接对照。

两个 288×512 并排是 576px 宽，桌面端绰绰有余（原来单屏时两侧大片留白）。
移动端按视口等比缩放，390px 的手机上每边约 190px 宽、338px 高，两屏仍能同屏看到 ——
而这正是"对照"的全部意义，比上下堆叠要好。

> 这个方案比"一个画面两只鸟"更好，还顺带消掉一个技术复杂度：
> **AI 有自己独立的画布，它的观测天然就是干净的**（画面里只有它自己那只鸟）。
> 合屏方案为了不让 AI 看到玩家的鸟，还得额外维护一个离屏画布。

### 公平性：不做平衡，这就是卖点

实测数据：**AI 平均 78.2 根，人类平均 6.8 根**（同一环境，20 局）。
AI 会碾压绝大多数人 —— **这是刻意保留的**，不加反应延迟、不降难度。
目的就是让人直观感受到这个模型有多强。

由此推出两条界面要求：

- **玩家出局要能瞬间重开**（空格 / 点击），不要有"Game Over"停顿动画
- **玩家死后 AI 继续飞**，画面上让差距持续拉大
- 计分板常驻：「你的本局 / 你的历史最佳 / AI 本局」

## 五、模型选哪个

有两条路，取舍在「故事强度」和「工作量」之间。

| | 路线 A：像素 CNN | 路线 B：状态向量 MLP |
|---|---|---|
| 模型 | 1,258,659 参数，fp16 2.5 MB | **18,051 参数，72 KB** |
| 推理运行时 | ONNX Runtime Web 或手写卷积 | **约 30 行 JS，零依赖** |
| 需要复刻渲染 | 是（已验证可行） | **否** |
| 卖点 | **「从原始像素学会玩」** | 「给了坐标之后学会玩」 |
| 工作量 | 高 | 低 |

**建议走 A。** 这个项目的全部叙事都建立在「从像素学」上，
换成喂坐标会把最有说服力的部分抽掉。而今天验证过之后，A 的主要风险已经消除。

`experiments/state_mlp.py` 里的 MLP 留作备选 —— 如果 A 卡住了，B 一天能上线。

---

## 六、分阶段实施

每一阶段都能独立产出可展示的东西，随时可以停。

**阶段 1：游戏本体（JS）**
- Canvas 重写物理与渲染，参数照抄 `game/flappy_env.py`
- 键盘 + 触摸控制，一键重开
- **验收**：同种子下 Python 与 JS 的管道序列逐根一致

**阶段 2：AI 上场**
- 导出 ONNX → fp16
- 离屏画布 + 覆盖判定降采样 + 4 帧栈
- **验收**：同种子同初始状态下，JS 与 Python 的动作序列一致；
  浏览器里跑 30 局，平均过管道数落在 Python 的 70–85 区间

**阶段 3：同台竞技 + 讲解**
- 双鸟同图、计分板、重开
- 英文讲解：RL 是什么、Q/V/A 是什么、这个网络长什么样
  （中文版已有，见 `docs/learn/03-dueling.md` 和那张架构图，翻译即可）
- 移动端布局

**阶段 4：发布**
- Cloudflare Pages + 仓库自动部署
- OG 卡片图（用 demo 视频的一帧）
- LinkedIn 帖子：视频 + 结论数字 + 评论区放链接

---

## 七、内容清单（英文，需要新写）

页面上除了游戏，还需要这些文字。中文素材大多已有，主要是翻译 + 精简：

- 一句话钩子：这是什么、为什么值得看
- **RL 基础**：状态 / 动作 / 奖励 / Q 值，三四句话讲完
- **这个网络**：Dueling 双头，V 和 A 分别是什么（架构图可直接复用）
- **训练结果**：78.2 根 vs 人类 6.8 根；域随机化让它在没见过的地图上也能飞
- **一句最有分量的话**：同样的算法，上一版跑 10 万局停在 1.3 根；
  差距全在数据管线的十来个结构性 bug 上 —— 这比任何调参故事都更能说明工程能力
- 指向 GitHub 仓库的链接

---

## 八、已定的决策

1. **模型走路线 A（像素 CNN）** —— 叙事核心是「从原始像素学」，不能换成喂坐标。
2. **不给 AI 任何削弱。** 不加反应延迟、不降难度、不改推理逻辑。
   AI 就用 `models/final_v1_best.pt` 的原始行为跑，目的就是让人直观感受到它有多强
   （实测 AI 78.2 根 vs 人类 6.8 根）。
   - 由此推出的界面要求：**玩家出局要能瞬间重开**（空格 / 点击），
     且玩家死后 AI 继续飞，让差距可见 —— 这是卖点，不是缺陷。
   - 计分板要同时显示「你的本局 / 你的最佳 / AI 本局」，让差距一直在画面上。
3. **在独立分支上做**：`demo/web`，不动主线。
4. **托管 Cloudflare Pages**，域名先不买。

## 九、仍待决定

- 英文文案由谁起草（目前按「先由 agent 起草、你再润色」推进）—— 页面讲解
  （`web/index.html` 的 `#learn` 区块）和 LinkedIn 帖子（`web/LINKEDIN_POST.md`）
  草稿都已由 agent 写好，**还需要你过一遍润色语气**，尤其是帖子正文的钩子句。
- Cloudflare Pages 的实际部署（dashboard 操作，见 `web/DEPLOY.md`）和买不买
  自定义域名，需要人工去 Cloudflare 控制台完成——agent 没有账号访问权限。
